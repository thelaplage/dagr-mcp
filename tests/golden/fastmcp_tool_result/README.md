# FastMCP ToolResult Projection Golden

`tests/test_fastmcp_projection_golden.py` records the Python golden digest for one real FastMCP `ToolResult` projected through `fastmcp.tool_result.v1`.

The pending TypeScript comparison will join this golden when WP3B lands.
