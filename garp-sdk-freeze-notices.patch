diff --git a/garp_sdk/enforcement_harness.py b/garp_sdk/enforcement_harness.py
--- a/garp_sdk/enforcement_harness.py
+++ b/garp_sdk/enforcement_harness.py
@@ -0,0 +1,5 @@
+# PUBLIC CANONICAL NOTICE
+# This module is frozen at the DAGR carve boundary. Public-canonical behavior
+# now lives in the dagr-mcp repository. Review private consumers for migration
+# within 90 days after the public launch.
+
diff --git a/garp_sdk/tool_call_disposition.py b/garp_sdk/tool_call_disposition.py
--- a/garp_sdk/tool_call_disposition.py
+++ b/garp_sdk/tool_call_disposition.py
@@ -0,0 +1,5 @@
+# PUBLIC CANONICAL NOTICE
+# This module is frozen at the DAGR carve boundary. Public-canonical behavior
+# now lives in the dagr-mcp repository. Review private consumers for migration
+# within 90 days after the public launch.
+
diff --git a/garp_sdk/policy_profile.py b/garp_sdk/policy_profile.py
--- a/garp_sdk/policy_profile.py
+++ b/garp_sdk/policy_profile.py
@@ -0,0 +1,5 @@
+# PUBLIC CANONICAL NOTICE
+# This module is frozen at the DAGR carve boundary. Public-canonical behavior
+# now lives in the dagr-mcp repository. Review private consumers for migration
+# within 90 days after the public launch.
+
diff --git a/garp_sdk/lint/matter_scope.py b/garp_sdk/lint/matter_scope.py
--- a/garp_sdk/lint/matter_scope.py
+++ b/garp_sdk/lint/matter_scope.py
@@ -0,0 +1,5 @@
+# PUBLIC CANONICAL NOTICE
+# This module is frozen at the DAGR carve boundary. Public-canonical behavior
+# now lives in the dagr-mcp repository. Review private consumers for migration
+# within 90 days after the public launch.
+
diff --git a/garp_sdk/sdk_spine.py b/garp_sdk/sdk_spine.py
--- a/garp_sdk/sdk_spine.py
+++ b/garp_sdk/sdk_spine.py
@@ -0,0 +1,5 @@
+# PUBLIC CANONICAL NOTICE
+# This module is frozen at the DAGR carve boundary. Public-canonical behavior
+# now lives in the dagr-mcp repository. Review private consumers for migration
+# within 90 days after the public launch.
+
diff --git a/garp_sdk/mcp_record_custody_gateway.py b/garp_sdk/mcp_record_custody_gateway.py
--- a/garp_sdk/mcp_record_custody_gateway.py
+++ b/garp_sdk/mcp_record_custody_gateway.py
@@ -0,0 +1,5 @@
+# PUBLIC CANONICAL NOTICE
+# This module is frozen at the DAGR carve boundary. Public-canonical behavior
+# now lives in the dagr-mcp repository. Review private consumers for migration
+# within 90 days after the public launch.
+
